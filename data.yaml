# 注意：这里建议用绝对路径，防止出错
# 你可以用 pwd 命令查看 yolo_dataset 的完整路径
path: ./yolo_dataset  # 数据集根目录
train: images/train   # 训练集图片位置 (相对于path)
val: images/val       # 验证集图片位置 (相对于path)

# 类别数
nc: 1

# 类别名称 (和你标注时的名字要一致)
names:
  0: rivet